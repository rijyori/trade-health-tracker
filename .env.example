# API 키는 이제 .env가 아니라 대시보드 우측 상단 톱니바퀴 → 설정 모달에서 등록/삭제합니다.
# 여기 아래 두 줄은 "맨 처음 이 프로젝트를 셋업할 때 이미 .env로 키를 넣어뒀던 경우"를 위한
# 1회성 마이그레이션용입니다 — 서버가 처음 뜰 때 credentials 테이블이 비어있으면 자동으로
# DB로 옮기고, 그 뒤로는 이 값을 더 이상 참조하지 않습니다. 새로 설정할 땐 UI를 쓰세요.
GATEIO_API_KEY=
GATEIO_SECRET_KEY=
GATEIO_LABEL=main
